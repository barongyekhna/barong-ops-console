services:
  backend:
    build:
      context: ./backend
    environment:
      APP_NAME: barong-ops-console-backend
      APP_ENV: development
      APP_VERSION: 0.1.0
    ports:
      - "8000:8000"
