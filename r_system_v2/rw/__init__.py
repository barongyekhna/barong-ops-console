"""R System v2 Warehouse runtime module.

This package is scoped to R-W only. It runs in mock mode by default and does
not invoke R-A, AI providers, Serper, or the real Keepa API.
"""

from __future__ import annotations

RW_MODULE_VERSION = "2.0.0-mock-ready"

